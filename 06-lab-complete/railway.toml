[build]
builder = "DOCKERFILE"

[deploy]
# Dùng CMD trong Dockerfile (đã handle $PORT qua sh -c)
# startCommand cố định gây bug expand $PORT với Dockerfile builder
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
